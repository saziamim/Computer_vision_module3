**Requirements:**

Python 3.10
NumPy
OpenCV
Matplotlib

**Install dependencies:** pip install numpy opencv-python matplotlib


**How rto Run:**
1. Place a grayscale image in the project directory (or update the image_path variable in the script).
2. Run: python blur_image.py
